import { Component } from 'react';
import styles from './SubContent.module.css';

class SubContent extends Component {
    render = () => {
        return (<div className={styles.subContent}/>
        );
    }
};

export default SubContent;